cache_root_dir: /home/gitlabci/buildcache/hero
disable_env_var: FULL_CI_TEST

artifacts:
  tc-pulp:
    inputs:
      - Makefile
      - setup.sh
      - toolchain/build.sh
      - toolchain/patches
      - toolchain/pulp.config
    outputs:
      - install
